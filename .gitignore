# ── OS / editor cruft ────────────────────────────────────────────────
.DS_Store
*.swp
*~

# ── Scratch analysis / review notes (not part of the plugin) ──────────
# 复盘/优化笔记（如 0711_opt.md），随手记录、不入库。
*_opt.md
# 临时取证/探测脚本，也不入库。
.research/
# 打补丁脚本留下的自动备份（scripts/xxx.sh.bak-20260818-004328）。原文件本身在 git
# 里，备份只是本地逃生副本；一旦入库就会被 run-track-a.sh 的 `git add -A` 自主提交带走。
*.bak-[0-9]*

# ── Track A driver logs ───────────────────────────────────────────────
# run-track-a.sh 的日志默认写到 $TMPDIR，这里再兜一层，防止意外落到仓库内。
.track-a-logs/
autopilot/.run-active
autopilot/changes/*/.lock/

# ── IDE 自动生成物（RepoWiki 等）─────────────────────────────────
# 勿入库；否则 run-track-a.sh 的 git add -A 自主提交会被大量生成文件污染（C12）。
.qoder/
