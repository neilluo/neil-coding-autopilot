# ── OS / editor cruft ────────────────────────────────────────────────
.DS_Store
*.swp
*~

# ── Scratch analysis / review notes (not part of the plugin) ──────────
# 复盘/优化笔记（如 0711_opt.md），随手记录、不入库。
*_opt.md

# ── Track A driver logs ───────────────────────────────────────────────
# run-track-a.sh 的日志默认写到 $TMPDIR，这里再兜一层，防止意外落到仓库内。
.track-a-logs/
autopilot/.run-active

# ── IDE 自动生成物（RepoWiki 等）─────────────────────────────────
# 勿入库；否则 run-track-a.sh 的 git add -A 自主提交会被大量生成文件污染（C12）。
.qoder/
